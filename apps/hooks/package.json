{
  "name": "apps",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "tsc -b && node dist/index.js",
    "dev": "tsc -b && node dist/index.js",
    "build": "tsc -b"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "esbuild": "0.24.0"
  },
  "dependencies": {
    "@repo/db": "*",
    "express": "^5.1.0"
  }
}